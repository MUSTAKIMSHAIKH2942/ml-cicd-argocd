apiVersion: apps/v1
kind: Deployment
metadata:
  name: ml-app
  labels:
    app: ml-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ml_app
  template:
    metadata:
      labels:
        app: ml_app
    spec:
      containers:
      - name: ml_app
        image: your-dockerhub-bytesbuddy/ml-cicd-app:latest
        ports:
        - containerPort: 5000
